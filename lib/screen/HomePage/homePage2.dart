import 'package:flutter/material.dart';

class HomePageFeeds extends StatelessWidget {
  static const routeName = '/homePageFeeds';
  const HomePageFeeds({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
